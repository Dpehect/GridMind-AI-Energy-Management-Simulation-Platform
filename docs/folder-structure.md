# Folder Structure

The codebase follows feature ownership. Shared primitives belong in `components/ui`; domain-specific modules belong in `features`; route files compose modules but do not own business logic.
