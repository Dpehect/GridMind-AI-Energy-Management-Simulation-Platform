# GridMind Enterprise Deployment Assets

Included:

- Production multi-stage Docker image
- Docker Compose with app, worker, scheduler and backup services
- Nginx reverse proxy example
- systemd units
- graceful shutdown
- schema compatibility check
- preflight validation
- restore smoke test
- rollback and release scripts

Review all paths, hostnames and secrets before production deployment.
