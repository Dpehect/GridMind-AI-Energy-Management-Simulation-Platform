# Contributing

1. Create a focused branch.
2. Keep domain logic outside route components.
3. Add validation for every external input.
4. Run `npm run check` and `npm run build`.
5. Document architectural decisions that affect future phases.

Use conventional commits: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, and `chore:`.
