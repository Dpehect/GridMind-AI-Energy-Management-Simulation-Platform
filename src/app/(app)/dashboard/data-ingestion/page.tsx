import { DatabaseZap, FileCheck2, Gauge, History } from "lucide-react";
import { PageHeader } from "@/components/ui/page-header";
import { MetricCard } from "@/components/ui/metric-card";
import { SectionCard } from "@/components/ui/section-card";
import { CsvImporter } from "@/features/data-ingestion/csv-importer";
import { ManualReadingForm } from "@/features/data-ingestion/manual-reading-form";

export default function DataIngestionPage(){return <main className="mx-auto max-w-7xl space-y-6 p-4 sm:p-6 lg:p-8"><PageHeader eyebrow="Data operations" title="Energy data ingestion" description="Validate, preview and commit meter readings without external services or API keys."/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Input modes" value="2" icon={DatabaseZap}/><MetricCard label="Max CSV batch" value="10k" icon={FileCheck2}/><MetricCard label="Validation" value="Strict" icon={Gauge}/><MetricCard label="Audit trail" value="Enabled" icon={History}/></div><div className="grid gap-6 xl:grid-cols-[1.45fr_.75fr]"><SectionCard title="CSV import workspace" description="Required columns: meterSerial, capturedAt and value. Quality is optional."><CsvImporter/></SectionCard><SectionCard title="Manual reading" description="Add a single validated measurement to an existing meter."><ManualReadingForm/></SectionCard></div><SectionCard title="Data quality policy" description="GridMind rejects malformed dates and negative readings, detects duplicates inside each file, verifies meter identities before commit, and records every successful batch in the local audit log."><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-muted/40 p-4"><p className="font-medium">Safe parsing</p><p className="mt-1 text-sm text-muted-foreground">Quoted cells and common column aliases are supported.</p></div><div className="rounded-2xl bg-muted/40 p-4"><p className="font-medium">Atomic batches</p><p className="mt-1 text-sm text-muted-foreground">Invalid identities stop the batch before insertion.</p></div><div className="rounded-2xl bg-muted/40 p-4"><p className="font-medium">Local-first</p><p className="mt-1 text-sm text-muted-foreground">No upload leaves the running GridMind installation.</p></div></div></SectionCard></main>}
