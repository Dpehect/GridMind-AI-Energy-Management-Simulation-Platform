import { notFound } from "next/navigation";
import { ArrowLeft, Building2, Gauge, Layers3, MapPin, RadioTower, Users, Zap } from "lucide-react";
import Link from "next/link";
import { buildings, floors, assets } from "@/data/assets";
import { StatusBadge } from "@/components/ui/status-badge";
import { SectionCard } from "@/components/ui/section-card";
export default async function BuildingDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const b=buildings.find(x=>x.id===id);if(!b)notFound();const linked=assets.filter(a=>a.building===b.name);return <main className="mx-auto max-w-7xl space-y-6 p-4 sm:p-6 lg:p-8"><Link href="/dashboard/buildings" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/>Back to buildings</Link><section className="overflow-hidden rounded-3xl border bg-card"><div className="relative p-6 sm:p-8"><div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-primary via-cyan-400 to-emerald-400"/><div className="flex flex-col justify-between gap-5 md:flex-row"><div><div className="flex items-center gap-3"><div className="flex size-12 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Building2/></div><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">{b.code}</p><h1 className="text-2xl font-semibold sm:text-3xl">{b.name}</h1></div></div><p className="mt-5 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>{b.address}</p></div><StatusBadge status={b.status}/></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Kpi icon={<Gauge/>} label="Efficiency" value={`${b.efficiency}%`}/><Kpi icon={<Users/>} label="Occupancy" value={b.occupancy.toLocaleString()}/><Kpi icon={<Zap/>} label="Monthly consumption" value={`${(b.monthlyKwh/1000).toFixed(1)} MWh`}/><Kpi icon={<RadioTower/>} label="Connected meters" value={String(b.meters)}/></div></div></section><div className="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><SectionCard title="Floor hierarchy" description="Operational zones structured by floor and function."><div className="space-y-3">{floors.slice(0,b.floors).map(f=><div key={f.level} className="rounded-2xl border p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><Layers3 className="size-4 text-primary"/><div><p className="font-medium">{f.name}</p><p className="text-xs text-muted-foreground">{f.area.toLocaleString()} m²</p></div></div><span className="rounded-lg bg-muted px-2 py-1 text-xs">Level {f.level}</span></div><div className="mt-3 flex flex-wrap gap-2">{f.zones.map(z=><span key={z} className="rounded-full border bg-background px-3 py-1 text-xs text-muted-foreground">{z}</span>)}</div></div>)}</div></SectionCard><SectionCard title="Connected assets" description="Priority equipment assigned to this building."><div className="space-y-3">{linked.length?linked.map(a=><div key={a.id} className="rounded-2xl border p-4"><div className="flex justify-between gap-3"><div><p className="text-sm font-medium">{a.name}</p><p className="mt-1 text-xs text-muted-foreground">{a.assetTag} · {a.zone}</p></div><span className="text-sm font-semibold text-primary">{a.health}%</span></div></div>):<p className="text-sm text-muted-foreground">No priority assets assigned.</p>}</div></SectionCard></div></main>}
function Kpi({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="rounded-2xl bg-muted/50 p-4"><div className="text-primary [&_svg]:size-4">{icon}</div><p className="mt-3 text-xl font-semibold">{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></div>}
