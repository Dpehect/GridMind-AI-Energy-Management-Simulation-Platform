{
  "scripts": {
    "env:validate": "tsx scripts/validate-environment.ts",
    "runtime:doctor": "tsx scripts/runtime-doctor.ts",
    "check:routes": "tsx scripts/check-duplicate-routes.ts",
    "check:imports": "tsx scripts/check-import-case.ts",
    "check:runtime": "npm run env:validate && npm run check:routes && npm run check:imports",
    "check": "npm run check:runtime && npm run typecheck && npm run lint && npm run test && npm run format:check"
  }
}
